# Structured Agent Request Fixtures

Paste the contents of any `*_screen_2_agent_request.json` file as the user message content when testing DSS Structured Agent `NkBiV9OM-v2`.

These fixtures are generated from `scripts/artifacts/policy_artifacts/policy_artifacts.csv` and the corrected scope selections in `scripts/artifacts/fixtures/parameter_config.py`.
